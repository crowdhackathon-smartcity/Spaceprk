using System;

namespace SpacePrk.Helpers
{
    public enum CarType
    {
        Small,
        Medium,
        Big
    };

    public enum DistanceType
    {
        Kilometers,
        Miles
    }
}